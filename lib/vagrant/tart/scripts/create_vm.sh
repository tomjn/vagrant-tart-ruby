#!/usr/bin/env bash

set -eo pipefail

VM_NAME=$1

# create a VM, this does not start the VM instead of using tart create,
# we need to unpackage the box file into the Tart VM folder with the appropriate VM name

echo "Not Implemented"
return 1
